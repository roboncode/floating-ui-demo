import { defineConfig } from 'vite'

export default defineConfig({
  build: {
    // minify: false,
    // rollupOptions: {
    //   output: {
    //     manualChunks: {
    //       'floating-ui': ['../src/floating-ui/dom'],
    //     },
    //   },
    // },
  },
})
